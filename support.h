#ifndef SUPPORT_INCLUDED
#define SUPPORT_INCLUDED

#include "provided.h"

bool win(Scaffold s, int &color, int N);	// Check if a player has won the game
bool gameOver(int& winner, Scaffold s, int N);

#endif	// SUPPORT_INCLUDED
